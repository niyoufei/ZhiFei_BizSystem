# 复制为 .env 后按需填写（不要提交 .env 到版本库）

# 服务端口（默认 8000）；若被占用可改为 8080 等
# PORT=8000

# API 认证：多个 key 用逗号分隔；不设置则开发模式（不校验）
# 兼容旧格式：API_KEYS=your-admin-key-1,your-admin-key-2
# 推荐角色格式：API_KEYS=admin:your-admin-key,ops:your-ops-key,readonly:your-readonly-key

# 历史 spark 兼容说明：
# `--mode spark` 和 `EVOLUTION_LLM_BACKEND=spark` 仍可作为旧别名使用，
# 但当前真实 provider 已统一映射到 OpenAI，不再读取 Spark 凭证作为评分主凭证。

# 学习进化 LLM 后端：rules（仅规则）| openai | gemini | auto
# 若你仍写 EVOLUTION_LLM_BACKEND=spark，系统会按兼容别名映射到 openai
# 设为 auto 或不显式指定时，系统会优先走 OpenAI，失败后自动回退 Gemini（若已配置）
# 设为 openai / gemini 时，也会优先该 provider，并在其失败后自动回退到另一 provider（若已配置）
# 若你提供多个不同账号，可用 OPENAI_API_KEYS / GEMINI_API_KEYS 逗号分隔配置账号池；
# 系统会在同 provider 内优先切号，短时失败账号会进入冷却，再跨 provider fallback。
# 默认推荐模型：OpenAI gpt-5.4、Gemini gemini-1.5-pro
# 可通过 OPENAI_MODEL / GEMINI_MODEL / SPARK_MODEL 覆盖；其中 SPARK_MODEL 仅作为 OPENAI_MODEL 的旧别名
# EVOLUTION_LLM_BACKEND=auto
# OPENAI_API_KEY=your-openai-key
# OPENAI_API_KEYS=your-openai-key-1,your-openai-key-2
# OPENAI_MODEL=gpt-5.4
# GEMINI_API_KEY=your-gemini-key
# GEMINI_API_KEYS=your-gemini-key-1,your-gemini-key-2
# GEMINI_MODEL=gemini-1.5-pro
# SPARK_MODEL=gpt-5.4
# EVOLUTION_LLM_ACCOUNT_COOLDOWN_SECONDS=300

# 限流（可选）
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_DEFAULT=100/minute
# RATE_LIMIT_SCORE=30/minute
# RATE_LIMIT_UPLOAD=20/minute
